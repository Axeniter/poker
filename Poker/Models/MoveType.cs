namespace Poker.Models
{
    /// <summary>
    /// Represents player move in round
    /// </summary>
    public enum MoveType
    {
        Fold,
        Check,
        Call,
        Bet,
        Raise
    }
}
